version: '3.8'
services:
  vitepress:
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - ./:/usr/src/app
        - node_modules:/usr/src/app/node_modules
    command: yarn run docs:dev