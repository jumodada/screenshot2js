version: '3.8'
services:
  vitepress:
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - "8080:8080"
    volumes:
      - ./:/usr/src/app
        - node_modules:/usr/src/app/node_modules
    command: yarn run docs:dev